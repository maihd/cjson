script: make

branches:
  only:
    - master
    - stable

matrix:
  include:
    - os: osx 
      language: c
      script: make
    
    - os: linux
      language: c
      script: make

    - os: windows
      language: c
      before_install: choco install make
      script: make

    - os: windows
      language: c
      script:
        - cmd /c .travis/build_vs2017.bat